import {StyleSheet} from 'react-native';

const InputStyles = StyleSheet.create({
  input: {
    backgroundColor: '#FFF',
    paddingVertical: 12,
    paddingHorizontal: 8,
    borderRadius: 5,
    borderWidth: 1,
    // borderColor: '#bdbdbd',
  },
});

export default InputStyles;
